{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Server (Debug)",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/cmd/server/main.go",
            "cwd": "${workspaceFolder}",
            "env": {
                "GO_ENV": ".env"
            },
            "args": []
        }
    ]
}